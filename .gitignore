*.iml
.gradle/
/local.properties
.idea/
.DS_Store
build/
captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab
*.keystore
!debug.keystore
/keystore.properties
# Release signing material must never be committed -- it lives only in CI secrets.
*.jks
*.b64

# Model weights are never committed. They are downloaded at runtime.
*.gguf
*.safetensors
*.onnx
/models/
